name: "Windows Portable Release Build"

on:
  push:
    tags: 
      - 'v*'
  workflow_dispatch:

jobs:
  build_windows_artifact:
    runs-on: windows-latest

    steps:
      - name: ⬇️ Checkout code (检出代码)
        uses: actions/checkout@v4
        
      # 【!!! 新增步骤：深度清理 Git 历史 !!!】
      - name: 🧹 Clean up Git History Cache (清理 Git 历史缓存)
        shell: powershell
        run: |
          Write-Host "正在清理 Go 工具可能读取的旧 Git 历史..."
          # 强制删除可能包含旧路径配置的 Go 缓存和模块信息
          if (Test-Path -Path $env:GOPATH) { Remove-Item -Path $env:GOPATH -Recurse -Force }
          # 重置 Git 目录，确保 Go 工具链无法从 Git 历史中找到旧的配置路径
          Remove-Item -Path ".git" -Recurse -Force

      # --- 1. 手动安装 Go 环境 ---
      
      - name: 🛠️ Setup Go environment Manually (手动安装 Go 环境)
        shell: powershell
        run: |
          $goVersion = "1.21.13" # 沿用之前的稳定版本
          Write-Host "正在手动下载并安装 Go $goVersion..."
          
          # 下载 Go 压缩包
          $goZip = "go$goVersion.windows-amd64.zip"
          $goUrl = "https://golang.org/dl/$goZip"
          Invoke-WebRequest -Uri $goUrl -OutFile $goZip
          
          # 解压到 C:\Go
          Expand-Archive -Path $goZip -DestinationPath "C:\" -Force
          
          # 设置环境变量
          $env:Path += ";C:\go\bin"
          $env:GOPATH = "C:\Users\runneradmin\go"
          Write-Host "Go 环境配置完毕。版本信息:"
          go version
          
      - name: 🛠️ Setup .NET Core (安装 .NET 环境)
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x' 

      # --- 2. 编译核心 subs-check.exe 内核 (Go) ---
      
      - name: ⚙️ Build subs-check.exe (编译 Go 内核)
        shell: powershell
        run: |
          # 确定当前工作区路径
          $Workspace = "${{ github.workspace }}"
          Set-Location $Workspace
          
          # 1. 整理和下载依赖 (go.mod 已在根目录)
          Write-Host "正在整理 Go 模块依赖..."
          go mod tidy
          
          # 2. 编译 Go 项目并输出到根目录
          # 使用 Invoke-Expression 确保命令被精确执行
          $BuildCommand = "go build -ldflags ""-s -w"" -o subs-check.exe main.go"
          Write-Host "执行编译命令: $BuildCommand"
          Invoke-Expression $BuildCommand
          
          # 检查编译结果
          if (-not (Test-Path subs-check.exe)) {
              Write-Error "错误：Go 内核编译失败，未生成 subs-check.exe 文件！"
              exit 1
          }

      # --- 3. 编译 subs-check.win.gui.exe (C# / .NET) ---
      
      - name: 🖥️ Build GUI and Dependencies (编译 GUI 和依赖文件)
        shell: powershell
        run: |
          $GuiProjectName = "subs-check.win.gui.sln" 
          $OutputFolder = "${{ github.workspace }}/gui_build_output"
          
          dotnet publish $GuiProjectName `
            --configuration Release `
            --runtime win-x86 `
            -o $OutputFolder

          # 移动编译结果和依赖DLLs到根目录
          Copy-Item -Path "$OutputFolder\subs-check.win.gui.exe" -Destination ${{ github.workspace }}
          Copy-Item -Path "$OutputFolder\Newtonsoft.Json.dll" -Destination ${{ github.workspace }}
          Copy-Item -Path "$OutputFolder\Newtonsoft.Json.xml" -Destination ${{ github.workspace }}
          Copy-Item -Path "$OutputFolder\YamlDotNet.dll" -Destination ${{ github.workspace }}
          Copy-Item -Path "$OutputFolder\YamlDotNet.xml" -Destination ${{ github.workspace }}
          

      # --- 4. 准备其他文件和目录结构 ---
      
      - name: 📦 Prepare Release Files (准备配置和预编译文件)
        shell: powershell
        run: |
          New-Item -ItemType Directory -Path config
          New-Item -ItemType Directory -Path output
          
          Copy-Item -Path Upgrade.exe -Destination ${{ github.workspace }}
          Copy-Item -Path subs-check_Windows_i386.zip -Destination ${{ github.workspace }}
          

      # --- 5. 压缩所有文件到 ZIP ---
      
      - name: 💾 Compress All Files (压缩文件)
        shell: powershell
        run: |
          $TagName = "${{ github.ref_name }}"
          $ZipName = "SubCheck-Portable-$TagName.zip"
          
          Compress-Archive `
            -Path subs-check.exe, subs-check.win.gui.exe, Upgrade.exe, `
                    Newtonsoft.Json.dll, Newtonsoft.Json.xml, `
                    YamlDotNet.dll, YamlDotNet.xml, `
                    subs-check_Windows_i386.zip, `
                    config, output `
            -DestinationPath $ZipName

      # --- 6. 上传到 Release Assets ---
      
      - name: 📤 Upload Release Asset (上传到 Release)
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: SubCheck-Portable-*.zip
          token: ${{ secrets.GITHUB_TOKEN }}
